// root = true
// Указывает, что это корневой файл EditorConfig.
// EditorConfig прекратит поиск настроек в родительских директориях.

// [*]
// Настройки по умолчанию для всех файлов

// charset = utf-8
// Кодировка файлов — UTF-8

// indent_style = space
// Использовать пробелы (вместо табов) для отступов

// indent_size = 2
// Размер отступа — 2 пробела

// end_of_line = lf
// Символ конца строки — LF (\n), Unix-стиль
// Это важно для совместимости между Linux/macOS/Windows и CI-системами

// [*.md]
// Настройки, специфичные для файлов Markdown (.md)

// trim_trailing_whitespace = false
// Не удалять пробелы в конце строк в Markdown-файлах
// Это полезно, потому что в Markdown два пробела в конце строки означают разрыв строки
